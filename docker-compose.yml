version: "3.9"

services:
  scraper:
    image: scraper
    container_name: scraper
    build:
      context: ./
    env_file: .env
    network_mode: "host"
    working_dir: /home/scraper/
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
